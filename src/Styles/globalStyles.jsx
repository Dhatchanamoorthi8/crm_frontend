// globalStyles.js
import { StyleSheet } from 'react-native'

export const globalStyles = StyleSheet.create({
  text: {
    fontFamily: 'MonaSans_400Regular', // Your default font family
    fontSize: 20, // Default font size (adjust as needed)
    color: '#000' // Default text color (optional)
  }
  // You can add other global styles here if needed
})
